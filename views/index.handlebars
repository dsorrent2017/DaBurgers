<h1>Burgers!</h1>

<h2>Burgers waiting to be devoured</h2>


<!-- 1. change sleep to devoured 
     2. change dl to devoured
     3. change burgers to burgers in 
      2 steps burgers}} and then  burgers/cat-block

      get rid of cat-block

 -->

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers Being Devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger </h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devouring?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devouring!<br>
    <input type="radio" name="devoured" value="0"> Available
  </div>

  <button type="submit">Add Burger</button>
</form>
